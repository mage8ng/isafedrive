# Render Blueprint for iSafeDrive (full-stack: API + passenger web + driver web + admin)
# Deploy: push this repo to GitHub, create a new "Blueprint" in Render, connect the repo.
# Service URLs (stable, permanent):
#   API:       https://isafedrive-api.onrender.com
#   Passenger: https://isafedrive-pax.onrender.com
#   Driver:    https://isafedrive-driver.onrender.com
#   Admin:     https://isafedrive-admin.onrender.com

databases:
  - name: isafedrive-db
    databaseName: isafedrive
    user: isafedrive
    plan: free
    postgresMajorVersion: 16

services:
  - type: web
    name: isafedrive-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install && npm run build -w packages/shared && npm run build -w apps/api
    startCommand: node apps/api/dist/main.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NODE_ENV
        value: development   # Render defaults to 'production', which disables TypeORM synchronize (no migrations exist)
      - key: PORT
        value: "3000"        # pin so pax/driver can reach it on the internal network at isafedrive-api:3000
      - key: DATABASE_URL
        fromDatabase:
          name: isafedrive-db
          property: connectionString
      - key: JWT_SECRET
        sync: false # set this in the Render dashboard (any long random string)
      - key: JWT_REFRESH_SECRET
        sync: false # set this in the Render dashboard (any long random string)

  - type: web
    name: isafedrive-pax
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: node apps/passenger-web/server.mjs
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: API_URL
        value: http://isafedrive-api:3000

  - type: web
    name: isafedrive-driver
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: node apps/driver-web/server.mjs
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: API_URL
        value: http://isafedrive-api:3000

  - type: web
    name: isafedrive-admin
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install && npm run build -w apps/admin-dashboard
    startCommand: npm run start -w apps/admin-dashboard
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: PORT
        value: "3100"
      - key: NEXT_PUBLIC_API_URL
        value: https://isafedrive-api.onrender.com/api/v1

  # NOTE: admin-dashboard is deployed here on Render (Next.js) for simplicity.
  # Vercel alternative: Root Directory = apps/admin-dashboard, Framework = Next.js,
  # build = next build, env NEXT_PUBLIC_API_URL = https://isafedrive-api.onrender.com/api/v1
