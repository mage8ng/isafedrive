name: isafedrive_driver
description: iSafeDrive driver app - receive ride requests, navigate, track earnings.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
